[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "expos-sdk"
version = "0.1.0"
description = "ExpOS Form ABI clients and deterministic development emulator"
requires-python = ">=3.10"
